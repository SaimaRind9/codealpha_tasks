# codealpha_tasks
ShopNest is a simple e-commerce web app built with Django and JavaScript.It includes product listings with search and category filters, an AJAX-based shopping cart, user registration and login, checkout with shipping details, order history with status tracking and an admin panel to manage products, users, and orders. SQLite is used as the database.
